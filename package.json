{
  "name": "scheduler",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "tsc",
    "prepare": "npm run clean && npm run build",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "debug": "^4.3.1",
    "redis": "^3.0.2"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/redis": "^2.8.28",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
